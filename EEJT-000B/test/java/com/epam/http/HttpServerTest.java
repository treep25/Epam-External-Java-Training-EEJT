package com.epam.http;

import com.epam.io.SocketIO;
import com.epam.product.ProductRepo;
import org.junit.jupiter.api.Test;

import java.net.Socket;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

class HttpServerTest {

//    @Test
//    void run() {
//        Socket socketMock = mock(Socket.class);
//        ProductRepo productRepoMock = mock(ProductRepo.class);
//        HttpServer httpServerMock = mock(HttpServer.class);
//        HttpServerHandler httpServerHandlerMock = mock(HttpServerHandler.class);
//        when(httpServerMock.creatThreadServerHttp(socketMock)).thenReturn(httpServerHandlerMock);
//        verify(httpServerHandlerMock);
//    }
}